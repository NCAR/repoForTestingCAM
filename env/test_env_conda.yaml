name: adf_test
channels:
  - conda-forge
  - defaults
  - ncar
dependencies:
  - pyyaml
  - scipy
  - cartopy
  - shapely
  - xarray
  - dask
  - matplotlib
  - pandas
  - netcdf4
  - geocat-comp
  - pre-commit  #Used for running pre-commit tests during development
  - pylint      #Used for running pre-commit/pylint during development
  - pytest      #Used for running python unit tests during development
  - python=3.10
prefix: /some/where/you/want/files/envs/adf_test
